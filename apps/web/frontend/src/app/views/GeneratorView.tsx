/**
 * Generator screen.
 *
 * Only the heading exists so far. Generating a postal code, the result, the
 * map, history, and advertising are the acceptance criteria of Issues #6
 * through #9 and arrive with their own acceptance tests.
 */
export const GeneratorView = () => {
  return <h1>{"Zipnami"}</h1>;
};
